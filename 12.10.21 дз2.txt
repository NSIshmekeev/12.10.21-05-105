#include <iostream>
using namespace std;
int fib(int k)
{
	if ((k == 1) || (k == 2)) {
		return 1;
	}
	else {
		return fib(k - 1) + fib(k - 2);
	}
}
int main() {
	setlocale(LC_ALL, "Russian");
	int a;
	cin >> a;
	cout << fib(a) << endl;
    system("pause");
	return 0;
}